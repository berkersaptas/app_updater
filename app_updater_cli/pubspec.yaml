name: app_updater_cli
description: >
  Cross-platform build and upload CLI for app_updater OTA releases and Dart-only Flutter patches.
version: 0.1.0
publish_to: 'none'
homepage: https://github.com/berkersaptas/app_updater
repository: https://github.com/berkersaptas/app_updater

environment:
  sdk: ^3.4.0

dependencies:
  args: ^2.5.0
  yaml: ^3.1.2
  http: ^1.2.2
  http_parser: ^4.0.2
  crypto: ^3.0.5
  path: ^1.9.0

executables:
  app_updater:

dev_dependencies:
  test: ^1.25.0
